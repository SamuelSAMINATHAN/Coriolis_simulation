[project]
name = "coriolis"
version = "0.1.0"
description = "Simulations of Coriolis force effects"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "pyvista>=0.40",
    "PyQt5>=5.15",
    "matplotlib>=3.10",
]

[tool.uv]
managed = true
